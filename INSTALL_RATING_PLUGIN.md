# SJTU 选课社区评分助手

> 在交大选课网站直接显示教师社区评分，悬停查看详情 — 本项目的核心功能

## 效果预览

```
┌─────────────────────────────────────────────────────┐
│  课程名称    教师      学分    学期                  │
├─────────────────────────────────────────────────────┤
│  数据结构    张三 ★4.8  4     2025-2026-1           │
│  操作系统    李四 ★4.2  4     2025-2026-1           │
│  高等数学    王五 ★3.9  5     2025-2026-1           │
└─────────────────────────────────────────────────────┘
```

每个教师名后面会出现一个彩色评分徽章，鼠标悬停可查看：

- 📊 评分分布柱状图
- 📈 学期评分趋势图
- 📚 教授课程列表（带评分进度条）
- 💬 最新点评摘要

## 完整安装步骤

### 第一步：爬取数据（首次使用）

如果你还没有爬取过数据，需要先运行一次爬虫：

```bash
cd sjtu-course-community-fetch

# 安装依赖
pip install -r requirements.txt

# 运行爬虫（会提示输入 SJTU 邮箱和密码）
python main.py scrape
```

爬虫会从 course.sjtu.plus 抓取教师、课程、点评数据，存入本地 SQLite 数据库 `sjtu_course.db`。首次爬取需要几分钟，之后可以运行增量更新补充新数据。

### 第二步：启动本地服务（每次使用前）

**这是必须的步骤。** 油猴脚本通过本地 API 查询评分数据，`web_app.py` 就是提供这个 API 的服务。

```bash
python web_app.py
```

启动后会看到：

```
启动 Web 服务器: http://localhost:8080
```

> ⚠️ **保持这个终端窗口不要关闭**，最小化即可。关闭后评分功能会失效。

启动后你可以顺便访问 http://localhost:8080 浏览 Web 仪表盘，查看数据概览、教师详情、课程详情等。

### 第三步：安装油猴脚本（一次性）

1. 在浏览器中安装 [Tampermonkey](https://www.tampermonkey.net/) 扩展
2. 点击浏览器右上角的 Tampermonkey 图标
3. 选择 **添加新脚本**（或"Create a new script"）
4. **删除编辑器中的所有默认内容**
5. 将 `sjtu-course-rating.user.js` 文件的全部内容复制粘贴进去
6. 按 `Ctrl+S` 保存

安装完成后脚本会自动启用，无需额外配置。

### 第四步：访问选课网站

打开 https://i.sjtu.edu.cn/xsxk/ 登录你的选课账号，评分徽章会自动出现在教师名后面。

## 日常使用流程

每次选课前：

1. 打开终端，运行 `python web_app.py`（保持运行）
2. 打开浏览器，访问选课网站
3. 评分徽章自动显示，鼠标悬停查看详情

## 快捷操作

| 操作 | 说明 |
|------|------|
| 鼠标悬停徽章 | 弹出教师详情窗口（评分分布、趋势、点评） |
| 点击徽章 | 跳转到本地 Web 仪表盘的教师详情页 |
| `Ctrl+Shift+R` | 强制刷新所有评分（清除缓存） |

## 设置菜单

点击 Tampermonkey 图标 → **SJTU 选课社区评分助手**：

- ⚙️ **设置 API 地址** — 默认 `http://localhost:8080`，一般不需要修改
- 🔄 **刷新评分** — 清除缓存并重新扫描页面

## 评分计算方式

- **加权平均**：新学期的评分权重更高
- **衰减公式**：每往前一个学期，权重衰减 30%
- **权重公式**：`weight = 0.7 ^ (当前学期序号 - 该学期序号)`

| 学期 | 相对权重 |
|------|----------|
| 2025-2026-1 | 100% |
| 2024-2025-2 | 70% |
| 2024-2025-1 | 49% |
| 2023-2024-2 | 34% |
| 2023-2024-1 | 24% |

这样设计的目的是让评分更准确地反映教师**近期**的教学水平。

## 更新数据

课程社区的点评数据会不断更新，建议定期重新爬取：

```bash
# 增量更新（只抓取新数据，推荐）
python main.py scrape

# 全量更新（重新抓取所有数据，耗时较长）
python main.py scrape --full
```

更新后重启 `web_app.py` 即可生效。

## 故障排除

### 评分徽章不显示

1. **确认 `python web_app.py` 正在运行** — 这是最常见的原因
2. 打开浏览器控制台（`F12` → Console）查看是否有报错
3. 确认 Tampermonkey 脚本已启用（图标上会显示脚本数量）
4. 确认已经爬取过数据（运行 `python main.py stats` 查看数据量）

### 徽章显示但弹窗加载失败

- 可能是本地服务响应较慢，等待几秒后重试
- 检查终端中 `web_app.py` 是否有报错信息

### 页面变卡

- 首次加载需要查询本地 API，会有短暂延迟
- 后续访问使用 10 分钟缓存，不会重复请求
- 如果仍然很慢，尝试减少页面上的课程数量

### 如何卸载

1. 点击 Tampermonkey 图标
2. 找到 "SJTU 选课社区评分助手"
3. 点击垃圾桶图标删除

## 技术细节

- 油猴脚本通过 `GM_xmlhttpRequest` 调用本地 `localhost:8080` API
- 使用 `MutationObserver` 监听选课页面的动态加载
- 图表通过 iframe 隔离渲染，避免选课页面污染 `Array.prototype.filter`
- 本地 API 从 SQLite 数据库（WAL 模式）查询数据，响应速度快
